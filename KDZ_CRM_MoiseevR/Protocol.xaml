<Window x:Class="KDZ_CRM_MoiseevR.Protocol"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KDZ_CRM_MoiseevR"
        mc:Ignorable="d"
        Title="Протокол" Height="286.093" Width="326.821">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGrid_Protocol" Grid.Column="1" Grid.Row="1"/>
        <Button x:Name="Back_Menu" Content="Назад в меню" HorizontalAlignment="Left" Margin="81,13,0,0" VerticalAlignment="Top" Width="115" Click="Back_Menu_Click" Grid.Column="1" Grid.Row="2"/>
        <Frame Name="FrameProtocol" NavigationUIVisibility="Hidden" Margin="0,0,0,131">

        </Frame>
    </Grid>
</Window>
